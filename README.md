# AutoAppsInstall01
This repository contains a PowerShell script that installs a predefined list of desktop applications using the Windows Package Manager (**winget**).  The script is intended for quick setup of a fresh Windows machine with common productivity and communication tools.
